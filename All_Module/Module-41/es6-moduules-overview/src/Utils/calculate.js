const add = (first, second) => {
    return first + second;
}
const multiply = (first, second) => {
    return first * second;
}
const divideTheFirstNumberByTheSecondNumber = (first, second) => {
    return first * second;
}
// export default add;
//multiple jinis export korar khetre
export {add, multiply, divideTheFirstNumberByTheSecondNumber};