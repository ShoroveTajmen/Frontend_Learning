const first = 'jaan';
const last = 'pakhi';
const greet = 'potas potas';

const name = first + ' ' + last + ' ' + greet;
console.log(name);

//let's using template string
const a = 10;
const b = 10;

const result = `the sum of ${a} and ${b} is ${a+b}`;
console.log(result);

//its easy to move next line using temlate stringg
const summary = `my name is shorove
i am 23 years old
i am studying css`;
console.log(summary);